# 项目规则

- 项目必须是标准 Xcode 工程
- 禁止使用 Swift Package 命令行、build.sh 或其他构建脚本
- 未经用户明确允许，不得运行应用
- 仅可使用 Xcode 工具执行构建检查并修复构建错误
- 未经用户明确允许，不得修改 Xcode Scheme 设置
- 用户要求提交、推送或确保工作区干净时，只允许执行对应操作，不得顺带修改工作区
- 遇到需要额外修改的问题时，必须先询问用户并取得明确同意
